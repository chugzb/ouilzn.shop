import type { StorageConfig } from '../types';

export const storageConfig: StorageConfig = {
  region: 'us-east-1',
  endpoint: undefined,
  accessKeyId: '',
  secretAccessKey: '',
  bucketName: '',
  publicUrl: undefined,
  forcePathStyle: true,
};
